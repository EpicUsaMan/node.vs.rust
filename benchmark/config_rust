#!/bin/sh

SERVER="http://localhost:8080/"

function PREPARE {
  pushd ../rust-server/
    diesel migration run
  popd
}

function RUN {
  ../rust-server/target/release/rust-server
}

function KILL {
  killall rust-server
}
